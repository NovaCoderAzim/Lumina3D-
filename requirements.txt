# Lumina3D backend core stack
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.31
pydantic>=2.5,<3
pydantic-settings>=2.1,<3
python-multipart>=0.0.9
python-dotenv>=1.0

# 3D Geometry and Math
trimesh>=4.0
numpy>=1.24

# Testing
pytest>=8.0
httpx>=0.27          # FastAPI TestClient dependency

# --- Real photogrammetry & AI modules ---
# Reconstruction: see reconstruction/requirements.txt (pycolmap, open3d, scipy)
# AI / Vision: see ai/requirements.txt (torch, ultralytics, opencv-python)
